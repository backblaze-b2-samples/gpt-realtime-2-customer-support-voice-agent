fastapi>=0.115.0
uvicorn[standard]>=0.34.0
python-multipart>=0.0.18
python-dotenv>=1.0.0
pydantic>=2.10.0
pydantic-settings>=2.7.0
boto3>=1.35.0
# Floor is the GA Realtime release that ships the non-beta
# `client.realtime.client_secrets.create(...)` mint API (POST
# /v1/realtime/client_secrets). The earlier beta endpoint
# (`client.beta.realtime.sessions.create`, POST /v1/realtime/sessions) was
# removed and now 404s. Verified against 1.109.1. If you bump past 2.x,
# re-check services/api/app/repo/openai_client.py::mint_realtime_session.
openai>=1.99.0,<2.0.0

# Dev / quality
ruff>=0.8.0
pytest>=8.0.0
pytest-asyncio>=0.24.0
httpx>=0.27.0
